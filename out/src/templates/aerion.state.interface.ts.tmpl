import { Project } from '../models/${inputName}s';
import { ProjectPage } from '../models/${inputName}Page';

export interface ProjectState {
  ${inputName}Page: ProjectPage;
  selectedProject: Project;
  isLoading: boolean;
  errorMessage: string;
  validationErrors: any[];
}
