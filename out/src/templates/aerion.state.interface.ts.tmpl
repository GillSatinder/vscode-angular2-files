import { ${modelName} } from '../models/${modelName.toLowerCase()}';
import { ${modelName}Page } from '../models/${modelName.toLowerCase()}Page';

export interface ${modelName}State {
  ${modelName.toLowerCase()}Page: ${modelName}Page;
  selected${modelName}: ${modelName};
  isLoading: boolean;
  errorMessage: string;
  validationErrors: any[];
}
